export enum RegistrationStatus {
    REG_SCREEN,
    VERIFICATION
}
export enum ResetPasswordStatus {
  RESET_PASSWORD,
  VERIFICATION,
  SUBMITNEWPASSWORD
}


export enum CheckUsernameExistsStatus {
  CHECKUSERNAME_EXISTS,
  VERIFICATION,
  GETUSERNAME
}

export enum RemoveDependantStatus {
  DEPENDANT_PROFILE,
  VERIFICATION,
  REMOVAL
}

export enum updatePasswordStatus {
  UPDATE_PASSWORD,
  VERIFICATION
}