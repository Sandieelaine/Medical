export interface Customer {
    _id?: any;
    billing_address: string;
    phone: String[];
    email: String[];
    first_name: string;
    last_name: string;
    title: string;
    joined_date: Date;
}